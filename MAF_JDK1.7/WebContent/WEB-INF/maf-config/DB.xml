<?xml version="1.0" encoding="euc-kr"?>
<configuration>
	/**
		기본 Dabase Connection Name
	*/
	<defaultConn>smis</defaultConn>
	
	/**
	driverClassName : 사용할 jdbc driver의 class name
	url : jdbc driver의해 연결할 connection에 대한 url
	username : connection을 연결할 사용자 이름
	password : connection을 연결할 사용자 비밀번호
	defaultAutoCommit : 풀에의해 생성된 커넥션의 auto-commit 상태
	defaultReadOnly : 풀에의해 생성된 커넥션의 read-only 상태
	                  jdbc에 따라 지원하지 않을수도 있다 (예 : informix)
	maxActive : 커넥션풀의 Active한 커넥션의 최대 갯수
	
	maxIdle : 커넥션풀의 idle한 커넥션의 최대 갯수
	minIdle : 사용되지 않고 풀에 저장될 수 있는 최소 커넥션 개수.
	maxWait : 커넥션이 풀에 반납되기까지 기다리는 최대 시간 (millisecond)
	testOnBorrow : true일 경우 커넥션 풀에서 커넥션을 가져올 때 커넥션이 유효한지의 여부를 검사한다.
	testWhileIdle : true일 경우 비활성화 커넥션을 추출할 때 커넥션이 유효한지의 여부를 검사해서
				 유효하지 않은 커넥션은 풀에서 제거한다.
				 사용되지 않는 커넥션을 검사할 때 유효하지 않은 커넥션은 검사하는 것이 좋다. 
	timeBetweenEvictionRunsMillis : 사용되지 않은 커넥션을 추출하는 쓰레드의 실행 주기를 지정한다. 
			양수가 아닐 경우 실행되지 않는다. 단위는 1/1000 초이다.
	minEvictableIdleTimeMillis : 사용되지 않는 커넥션을 추출할 때 이 속성에서 지정한 시간 이상 비활성화 상태인 커넥션만 추출한다. 
			양수가 아닌 경우 비활성화된 시간으로는 풀에서 제거되지 않는다. 
			시간 단위는 1/1000초이다.
			이 값을 알맞게 지정해서 사용되지 않는 커넥션을 풀에서 제거하는 것이 좋다. 
			커넥션의 동시 사용량은 보통 새벽에 최저이며 낮 시간대에 최대에 이르게 되는데 
			이 두 시간대에 필요한 커넥션의 개수 차이는 수십개에 이르게 된다. 
			이때 최대 상태에 접어들었더가 최소 상태로 가게 되면 풀에서 사용되지 않는 
			커넥션의 개수가 점차 증가하게 된다. 
			따라서 사용되지 않는 커넥션은 일정 시간 후에 삭제되도록 하는 것이 좋다. 
			보통 10~20분 단위로 사용되지 않는 커넥션을 검사하도록 지정하는 것이 좋다. 
	whenExhaustedAction : 커넥션 풀에서 가져올 수 있는 커넥션이 없을 때 어떻게 동작할지를 지정한다. 
	 		1 일 경우 maxWait 속성에서 지정한 시간만큼 커넥션을 구할 때 까지 기다리며, 
			0 일 경우 에러를 발생시킨다. 
			2 일 경우에는 일시적으로 커넥션을 생성해서 사용한다
		// url="jdbc:oracle:thin:@211.115.89.102:1522:ora9i"
		// url="jdbc:oracle:thin:@218.36.96.164:1522:ora920"
		// url="jdbc:oracle:thin:@218.36.96.165:1522:ora9i"
		// url="jdbc:oracle:thin:@203.234.247.19.19:1522:ora920"
		
	*/
	
	<conn name="edua1"
		driverClassName="oracle.jdbc.driver.OracleDriver"
		url="jdbc:oracle:thin:@220.75.183.3:1522:ora920"
		username="hkmca"
		password="qwer12"
		defaultAutoCommit="true"
		defaultReadOnly="true"
		maxActive="50"
		whenExhaustedAction="0"
		maxWait="1000">
	</conn>
	
</configuration>
